{
  "name": "doctui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.4.0",
  "engines": {
    "node": ">=24 <25"
  },
  "volta": {
    "node": "24.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "lint": "biome ci .",
    "format": "biome check --write .",
    "check": "biome check .",
    "typecheck": "tsc -p tsconfig.tools.json --noEmit && tsc -p packages/core/tsconfig.ts.json --noEmit && node scripts/vue-tsc.mjs -p packages/core/tsconfig.json --noEmit && node scripts/vue-tsc.mjs -p apps/storybook/tsconfig.json --noEmit && node scripts/vue-tsc.mjs -p apps/docs/tsconfig.json --noEmit",
    "test": "vitest run",
    "build:core": "bun run --filter @doctui/core build",
    "build:storybook": "bun run build:core && bun run --filter @doctui/storybook build",
    "build:docs": "bun run build:core && bun run --filter @doctui/docs build",
    "build": "bun run build:core && bun run --filter @doctui/storybook build && bun run --filter @doctui/docs build",
    "storybook": "bun run build:core && bun run --filter @doctui/storybook dev",
    "docs:dev": "bun run build:core && bun run --filter @doctui/docs dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/node": "^26.0.0",
    "@typescript/typescript6": "6.0.2",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vue/test-utils": "^2.5.0",
    "jsdom": "^30.0.0",
    "typescript": "7.0.2",
    "vite": "^8.3.0",
    "vitest": "^5.0.0",
    "vue": "3.5.42",
    "vue-tsc": "3.3.11"
  }
}
