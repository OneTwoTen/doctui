name: Temporary overlay contract sync

on:
  push:
    branches:
      - codex/issue-12-overlay

permissions:
  contents: write

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: codex/issue-12-overlay
          fetch-depth: 0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0
      - run: bun install --frozen-lockfile
      - name: Patch overlay public contract
        run: node scripts/patch-overlay-contract.mjs
      - name: Format changed sources
        run: bun x biome format --write packages/core/src/styles.css packages/core/src/component-metadata.ts apps/docs/.vitepress/config.mts
      - name: Regenerate metadata artifacts
        run: bun run metadata:generate
      - name: Commit synchronized contract
        run: |
          if git diff --quiet -- packages/core/src/styles.css packages/core/src/component-metadata.ts apps/docs/.vitepress/config.mts metadata/components.json llms.txt llms-full.txt; then
            echo "Overlay contract already synchronized"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add packages/core/src/styles.css packages/core/src/component-metadata.ts apps/docs/.vitepress/config.mts metadata/components.json llms.txt llms-full.txt
          git commit -m "docs(metadata): sync overlay contract"
          git push origin HEAD:codex/issue-12-overlay
